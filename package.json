{
  "name": "generator",
  "version": "1.0.0",
  "description": "Generate project",
  "main": "dist/app.js",
  "scripts": {
    "clean": "del-cli --force ./dist",
    "build": "tsc && copyfiles template/** dist -a",
    "lint": "eslint . --ext .ts",
    "lint-and-fix": "eslint . --ext .ts --fix",
    "start": "npm run clean && npm run build && node dist/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kulifay Zsolt",
  "license": "ISC",
  "dependencies": {
    "@types/prompts": "^2.0.9",
    "mustache": "^4.1.0",
    "prompt": "^1.1.0"
  },
  "devDependencies": {
    "@types/mustache": "^4.1.0",
    "@typescript-eslint/eslint-plugin": "^4.12.0",
    "@typescript-eslint/parser": "^4.12.0",
    "copyfiles": "^2.4.1",
    "del-cli": "^3.0.1",
    "eslint": "^7.17.0",
    "eslint-plugin-shopify": "^35.1.0",
    "typescript": "^4.1.3"
  }
}
